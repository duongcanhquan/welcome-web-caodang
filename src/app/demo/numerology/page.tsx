import { DemoNumerologyClient } from "@/components/demo/DemoNumerologyClient";

export const metadata = {
  title: "Demo — Thần số học (xem trước)",
  description: "Xem trước màn chờ quote và kết quả thần số học",
};

export default function DemoNumerologyPage() {
  return <DemoNumerologyClient />;
}
